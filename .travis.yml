language: php
php:
  - '7.1'
dist: trusty
sudo: false
before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction

deploy:
 provider: heroku
 api-key:
  secure: $API_KEY
 app: projetolaravel21
